import { SearchDirectoryPipe } from './search-directory.pipe';

describe('SearchDirectoryPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchDirectoryPipe();
    expect(pipe).toBeTruthy();
  });
});
