import { CustomersearchpipePipe } from './customersearchpipe.pipe';

describe('CustomersearchpipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomersearchpipePipe();
    expect(pipe).toBeTruthy();
  });
});
